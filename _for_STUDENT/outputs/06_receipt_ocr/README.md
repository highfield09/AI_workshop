# Workbook 6 · Your extracted results

Your script writes `first_receipt.xlsx` and `first_receipt_ocr.txt` here. Each output is local to your Codespace and Git-ignored.

Open the spreadsheet with **SpreadJS XLSX Editor** by **MESCIUS**, or use the read-only spreadsheet preview cell in Workbook 6. Microsoft Excel is not required on your laptop.

Compare every populated field with the original image before trusting it. Keep the image filename with the extracted row and retain the raw GLM-OCR response so errors can be traced. Flag omitted fields and normalised dates; do not guess missing values.
