{
    admin off
    auto_https off
}

:8080 {
    @openai_api path /v1 /v1/*

    handle @openai_api {
        reverse_proxy litellm:4000
    }

    handle {
        respond "Not found" 404
    }

    log {
        output stdout
        format json
    }
}
