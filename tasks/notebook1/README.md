# Notebook 1 output folder

Create `catalogue.html` here during Experiment 6. Ask GitHub Copilot Chat to return code, then create this file and paste the code yourself.

Preview it with **Show Preview** in VS Code. The page should load `../../data/notebook1/products.csv` and resolve image filenames from `../../data/notebook1/`.

`catalogue.html` is ignored by Git so each learner can experiment without committing personal work.
